general:
  artifacts:
    - dist/
dependencies:
  post:
    - gem install sass
    - npm install -g bower@1.4.1
    - npm install -g ember-cli@0.2.7
    - bower install
test:
  post:
    - ember build
    - aws s3 sync ./dist/* s3://ci-deploys.cid-labs.com/
