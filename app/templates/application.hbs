{{partial 'partials/pageheader'}}

{{outlet 'homepage'}}

{{outlet 'search'}}

{{#if showStreamer }}
  <main>
    {{ render 'header-nav' controller='application'}}
    <article class="stream__article new__stream__article--indent--left">
      {{outlet}}
    </article>
  </main>
{{/if}}

