<div class="buildermod__drawer buildermod__drawer--settings">
  <div class="settings__input__wrap">
    <label for="settings__slider--time" class="settings__slider">{{t 'graph_builder.settings.change_time'}}</label>
      {{input-slider
        elementId="settings__slider--time"
        type="time"
        years=years
        startDate=startDate
        endDate=endDate
        newStartDate=newStartDate
        newEndDate=newEndDate
        dateRange=dateRange
      }}
  </div>
</div>

