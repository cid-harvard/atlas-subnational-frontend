<article class="profile stream">
  <img src="assets/img/department_profile/baranquilla-hero.jpg" class="stream__img--hero">
  <header class="stream__header profile__header">
    <h2 class="stream__head profile__head">{{model.name}}</h2>
    <ol class="breadcrumbs">
      <li class="breadcrumb"><a href="#" class="breadcrumb__link">Location</a></li>
      <li class="breadcrumb"><a href="#" class="breadcrumb__link">Colombia</a></li>
      <li class="breadcrumb"><a href="#" class="breadcrumb__link">{{model.name}}</a></li>
      <li class="breadcrumb"><a href="#" class="breadcrumb__link">Profiles</a></li>
      <li class="breadcrumb"><a href="#" class="breadcrumb__link">Exports and imports</a></li>
    </ol>
  </header>
  <section class="stream__section">
    <header class="section__header">
      <h3 class="section__head">Overview</h3>
    </header>
    <ul class="list--section list--section--bullets">
      <li class="list__item--section--bullets">GDP grew {{format-percent model.gdpGrowth}} per year from {{model.yearRange}}</li>
      <li class="list__item--section--bullets">{{model.name}} has the 9th highest GDP per capita at ${{format-largenumber model.latestGdp}}</li>
      <li class="list__item--section--bullets">The population is {{format-largenumber model.latestPop}}, making {{model.name}} the 5th largest department</li>
    </ul>
    <table class="table--summaries">
      <thead>
        <tr class="table__row table__row--header">
          <th class="table__header"></th>
          <th class="table__header">All 32 departments</th>
          <th class="table__header">Value</th>
          <th class="table__header">Growth, annual (2009-2013)</th>
        </tr>
      </thead>
      <tbody>
        <tr class="table__row table__row--body">
          <td class="table__cell">GDP</td>
          <td class="table__cell">
            {{dot-plot xVar='realgdp' yVar=['dept_pop', 'realgdp'] data=model.profileDot elementId='dotplot--gdp' }}
          </td>
          <td class="table__cell">$8.2M</td>
          <td class="table__cell">{{spark-line yVar='realgdp' data=model.profileSpark elementId='spark--gdp' }} + 2.4%</td>
        </tr>
      </tbody>
    </table>
  </section>
  <section class="stream__section">
    <header class="section__header">
      <h3 class="section__head">Exports and imports</h3>
    </header>
    <ul class="list--section list--section--bullets">
      <li class="list__item--section--bullets">{{model.name}} exported USD $1.1B in 2013, 3% of Colombia's total exports</li>
      <li class="list__item--section--bullets">{{model.name}}'s main export partners in 2013 were the United States, China, and Venezuela</li>
      <li class="list__item--section--bullets">{{model.name}}'s main exports are metals, chemical products, and textiles</li>
    </ul>

    <div class="tabmod">
      {{#ivy-tabs}}
        {{#ivy-tab-list classNames='tabmod__tabs'}}
          {{#ivy-tab classNames='tabmod__tab' activeClass='tabmod__tab--is--active'}}
            <a class="tab__link">Exports, 2013</a>
          {{/ivy-tab}}
          {{#ivy-tab classNames='tabmod__tab' activeClass='tabmod__tab--is--active'}}
            <a class="tab__link">Export partners</a>
          {{/ivy-tab}}
          {{#ivy-tab classNames='tabmod__tab' activeClass='tabmod__tab--is--active'}}
            <a class="tab__link">Imports</a>
          {{/ivy-tab}}
          {{#ivy-tab classNames='tabmod__tab' activeClass='tabmod__tab--is--active'}}
            <a class="tab__link">Import partners</a>
          {{/ivy-tab}}
        {{/ivy-tab-list}}

        {{#ivy-tab-panel classNames='tabmod__card' activeClass='tabmod__card--is--active'}}
          <table class="table--summaries">
            <thead>
              <tr class="table__row table__row--header">
                <th class="table__header"></th>
                <th class="table__header">All 32 departments</th>
                <th class="table__header">Value</th>
                <th class="table__header">Growth, annual (2009-2013)</th>
              </tr>
            </thead>
            <tbody>
              <tr class="table__row table__row--body">
                <td class="table__cell">Total exports</td>
                <td class="table__cell">dot plot</td>
                <td class="table__cell">$8.2M</td>
                <td class="table__cell">line chart + 1.2%</td>
              </tr>
            </tbody>
          </table>
          <div class="vizmod">
            <div class="vizmod--treemod"></div>
            <div class="vizmod__list__wrap">
              <h4 class="vizmod__list__head">Top exports, all</h4>
              <ol class="vizmod__list">
                {{#each export in model.topExports}}
                  <li class="vizmod__list__item">{{export.name}}</li>
                {{/each}}
              </ol>
            </div>
          </div>
        {{/ivy-tab-panel}}

        {{#ivy-tab-panel classNames='tabmod__card' activeClass='tabmod__card--is--active'}}
          <h2>Export Partner</h2>
        {{/ivy-tab-panel}}

        {{#ivy-tab-panel classNames='tabmod__card' activeClass='tabmod__card--is--active'}}
          <h2>Imports </h2>
        {{/ivy-tab-panel}}

        {{#ivy-tab-panel classNames='tabmod__card' activeClass='tabmod__card--is--active'}}
          <h2>Import Partner</h2>
        {{/ivy-tab-panel}}
      {{/ivy-tabs}}
    </div>
  </section>
  <section class="stream__section">
    <header class="section__header">
      <h3 class="section__head">Export complexity</h3>
    </header>
    <table class="table--summaries">
      <thead>
        <tr class="table__row table__row--header">
          <th class="table__header"></th>
          <th class="table__header">All 32 departments</th>
          <th class="table__header">Value</th>
          <th class="table__header">Growth, annual (2009-2013)</th>
        </tr>
      </thead>
      <tbody>
        <tr class="table__row table__row--body">
          <td class="table__cell">Overall export complexity</td>
          <td class="table__cell">dot plot</td>
          <td class="table__cell">0.3</td>
          <td class="table__cell">line chart + TK%</td>
        </tr>
      </tbody>
    </table>
    <div class="stepper">
      <ol class="stepper__list">
        {{#each story in stepStories}}
          {{component 'department-stepper' story=story parentController=controller}}
        {{/each}}
      </ol>
      <div class="stepper__vizmod">
        <div class="stepper__vizmod__vizwrap"></div>
        <div class="stepper__controls">
          <button class="stepper__control stepper__control--left" {{action 'back'}}>Back</button>
          <p class="stepper__control__progress">{{activeStep}} of {{stepStories.length}}</p>
          <button class="stepper__control stepper__control--right" {{action 'forward'}}>Forward</button>
        </div>
      </div>
      <table class="table table--scroller table--complexity">
        <thead class="table__thead table__thead--complexity table__thead--scroller">
          <tr class="table__row table__row--complexity table__row--scroller">
            <th class="table__header table__header--scroller table__header--complexity">Current exports of {{model.name}}, ranked by complexity</th>
            <th class="table__header table__header--scroller table__header--complexity">Export value (2013)</th>
            <th class="table__header table__header--scroller table__header--complexity">Growth, annual (2009-2013)</th>
          </tr>
        </thead>
        <tbody class="table__tbody table__tbody--complexity table__tbody--scroller">
          {{#each export in model.currentExports }}
            <tr class="table__row table__row--complexity table__row--scroller">
              <td class="table__cell table__cell--scroller table__cell--complexity">{{export.name}}</td>
              <td class="table__cell table__cell--scroller table__cell--complexity">{{export.value}}</td>
              <td class="table__cell table__cell--scroller table__cell--complexity">{{export.growth}}</td>
            </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </section>
  <section class="stream__section">
    <header class="section__header">
      <h3 class="section__head">Export possiblities</h3>
    </header>
    <p class="section__intro">We've found that countries which export complex products grow faster than those which export simple products. Using the similarity map we built in the section above, we've highlighted  high potential products for {{model.name}}, ranked by which have the highest combination of opportunity and complexity.</p>
    <div class="vizmod">
      <div class="vizmod--treemod">
        <div class="vizmod__legend">
          <ul class="vizmod__legend__list">
            <li class="vizmod__legend__item">Non-export</li>
            <li class="vizmod__legend__item">Export</li>
            <li class="vizmod__legend__item">Similarity link</li>
          </ul>
        </div>
        <div class="vizmod__vizwrap"></div>
      </div>
      <div class="vizmod__list__wrap">
        <h4 class="vizmod__list__head">High potential exports for {{model.name}}</h4>
        <ol class="vizmod__list">
          {{#each export in model.topExports}}
            <li class="vizmod__list__item">{{export.name}}</li>
          {{/each}}
        </ol>
      </div>
      <footer class="vizmod__footer">
        <p class="vizmod__footer__p">Note that the list is dynamically generated. Not all exports may make sense given local conditions not captured in our measure of product similarity.</p>
      </footer>
    </div>
  </section>
  <div class="interstitial">
    <h3 class="interstitial__head">Continue reading</h3>
    <button class="interstitial__btn">Industries and manufacturing</button>
  </div>
  <div class="interstitial">
    <h3 class="interstitial__head">Share</h3>
    <div class="interstitial__btnmod">
      <button class="interstitial__btn">Facebook</button>
      <button class="interstitial__btn">Twitter</button>
    </div>
  </div>
  <div class="interstitial">
    <h3 class="interstitial__head">Download data for this profile</h3>
    <div class="interstitial__btnmod">
      <button class="interstitial__btn">CSV (13MB)</button>
      <button class="interstitial__btn">Excel (41MB)</button>
    </div>
  </div>
  <section class="stream__section">
    {{partial "partials/profile-navigator"}}
  </section>
</article>
<footer class="footer">
  <p class="footer__text">Footer TK</p>
</footer>
<!--
{{dot-plot
  xVar='realgdp_percap'
  data=model.profileDot
  elementId='income-capita-dotplot'
  height=100
  width=400
}}

{{spark-line
  yVar='realgdp_percap'
  data=model.profileSpark
  elementId='income-capita-sparkline'
  height=100
  width=400
}}

{{dot-plot
  xVar='dept_pop'
  yVar=["dept_pop", "realgdp"]
  data=model.profileDot
  elementId='population-dotplot'
  height=100
  width=400
}}

{{spark-line
  yVar='dept_pop'
  data=model.profileSpark
  elementId='population-sparkline'
  height=100
  width=400
}}

{{product-space
  data=model.profileExport
  elementId='product-space'
  height=300
  width=400
}}

{{product-table
  data=model.profileExport
  elementId='product-table'
  height=300
  width=400
}}
--!>
