import {
  formatLargenumber
} from '../../../helpers/format-largenumber';
import { module, test } from 'qunit';

module('FormatLargenumberHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatLargenumber(42);
  assert.ok(result);
});
