import { decimal } from '../../../helpers/decimal';
import { module, test } from 'qunit';

module('Unit | Helper | decimal');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = decimal(42);
  assert.ok(result);
});
